import { aboutShow } from "../../../assets"

const About = () => {
  return (
    <div style={{
      backgroundImage: `url(${aboutShow})`,
      width: "100vw",
      height: "98vh",
      position: "relative",
      backgroundSize: "cover"
    }}>
      back
    </div>
  )
}

export default About